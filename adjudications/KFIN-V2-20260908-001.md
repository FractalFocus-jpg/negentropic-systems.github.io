# Adjudication — KFIN-V2-20260908-001

**Date:** 2026-09-08  
**Raw workflow terminal:** `INCREMENTAL_KAPPA_CONTENT_PASS`  
**Controlling adjudicated terminal:** `PROCEDURAL_FAIL__PERSISTENCE_BASELINE_IMPLEMENTATION_MISMATCH__RAW_RECEIPT_PRESERVED`

## Reason

Post-run Codex review identified that the frozen protocol defined the persistence baseline as comparing current β against the training target high-volatility threshold `q80_train`, while `kappa_validation_v2.py` recomputed a q80 threshold from `train["beta"]` instead of using the already frozen `target_threshold`.

Those thresholds happened to be numerically equal in this execution (`0.19696579040212642`), so the recorded persistence metrics and raw pass condition are unchanged numerically. Nevertheless, the implementation did not literally implement the preregistered experiment. Under the first-terminal / no-retroactive-repair law, that is a procedural failure, not a scientific PASS.

Codex also identified that the script used `Path.write_text`, which could overwrite a local first receipt on repeat execution. The raw GitHub Actions artifact remains preserved and hash-bound, but successor code must fail closed on repeated attempts and use exclusive receipt creation.

## Preserved raw custody

- Workflow run: `34243432146`
- Execution commit: `480933cf6e339dc85dba93edf9bb24e91433c7d5`
- Artifact ID: `10062961206`
- Artifact ZIP SHA-256: `98f51bc17ab6b04a39a1d4dc06832dc5d6ab326abd62b0925d9ce69f1f634bc2`
- Raw receipt SHA-256: `40f325b1758eeeed465b57c2bdf85465aea591c0af4dd16a4cb3a8a427d8e3de`
- Console SHA-256: `3c0d03629689bef51ab2af2d5ef3e4d92209f53254f733d9f8eb27514a700af1`

## Successor rule

Do not modify and rerun `KFIN-V2-20260908-001` to win. A fresh successor must:

1. use the exact frozen target threshold for persistence;
2. preserve all scientific score/target/split/pass semantics otherwise;
3. create receipts exclusively rather than truncating existing ones;
4. refuse GitHub Actions rerun attempts for the same run identity;
5. preserve any implementation error separately from a scientific terminal;
6. undergo code review before confirmatory execution.

The fresh successor identity is `KFIN-V2_1-20260908-002`.
